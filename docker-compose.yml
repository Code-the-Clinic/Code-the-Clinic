version: '3.8'

services:
  # 1. The Project Website (MkDocs)
  docs:
    build: ./docs-site
    ports:
      - "8001:8000"  # Map container port 8000 to local (laptop) port 8001
    volumes:
      - ./docs-site:/docs # Syncs edits to project website

  # 2. The Backend (Django) - Placeholder for now
  # Uncomment this after setting up Django files
  # backend:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./backend:/app